# token配置
token:
  # 令牌自定义标识
  header: Authorization
  # 令牌秘钥
  secret: BACKEND_TOKEN_SECRET_202008181818!@#$%^&_PROD
  # 令牌有效期（默认30分钟）：4H
  expireTime: 240
#慢sql阈值
sql:
  slowSql: 1000
# 数据源配置
spring:
  # redis 配置
  redis:
    # 地址
    host: pointcard-redis-001.lyzymq.0001.ape1.cache.amazonaws.com
    # 端口，默认为6379
    port: 56379
    password:
    # 连接超时时间
    timeout: 10s
    lettuce:
      pool:
        # 连接池中的最小空闲连接
        min-idle: 0
        # 连接池中的最大空闲连接
        max-idle: 8
        # 连接池的最大数据库连接数
        max-active: 8
        # #连接池最大阻塞等待时间（使用负值表示没有限制）
        max-wait: -1ms
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driverClassName: com.mysql.cj.jdbc.Driver
    druid:
      # 主库数据源
      master:
        url: jdbc:mysql://pointcard-db.ckmsh7yn8ood.ap-east-1.rds.amazonaws.com:3306/point-card?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
        username: root
        password: whfajdf2382236ertyfLUU
      # 从库数据源
#      slave:
#        # 从数据源开关/默认关闭
#        enabled: true
#        url: jdbc:mysql://?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
#        username: root
#        password:
      # 初始连接数
      initialSize: 5
      # 最小连接池数量
      minIdle: 10
      # 最大连接池数量
      maxActive: 20
      # 配置获取连接等待超时的时间
      maxWait: 60000
      # 配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位是毫秒
      timeBetweenEvictionRunsMillis: 60000
      # 配置一个连接在池中最小生存的时间，单位是毫秒
      minEvictableIdleTimeMillis: 300000
      # 配置一个连接在池中最大生存的时间，单位是毫秒
      maxEvictableIdleTimeMillis: 900000
      # 配置检测连接是否有效
      validationQuery: SELECT 1 FROM DUAL
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      webStatFilter:
        enabled: true
      statViewServlet:
        enabled: true
        # 设置白名单，不填则允许所有访问
        allow:
        url-pattern: /druid/*
        # 控制台管理用户名和密码
        login-username:
        login-password:
      filter:
        stat:
          enabled: true
          # 慢SQL记录
          log-slow-sql: true
          slow-sql-millis: 1000
          merge-sql: true
        wall:
          config:
            multi-statement-allow: true
cthl:
  batch:
    count: 20
  upload:
    http:
      filepath: /home/ftpuser/www/
      outPutFolderPath: C:\Users\28678\Desktop\work\daima
    ftp:
      encoding: UTF-8
      enabled: true
      host:
      port: 30303
      username: ftpuser
      password: vgiuwe236skj
      filepath: /home/ftpuser/www/
      buffer-size: 4096
    filebrowse:


sentry:
  dsn: